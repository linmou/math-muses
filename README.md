# Math Muses

A frontend-only interactive math game and creative sandbox. No backend required.

## Local development

1. Install dependencies: `npm install`
2. Start the dev server: `npm run dev`
3. Build for production: `npm run build`
